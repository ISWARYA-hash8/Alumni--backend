spring.application.name=AlumniPortal

# -----------------------------
# Server
# -----------------------------
server.port=${PORT:8080}
# -----------------------------
# MySQL Configuration
# -----------------------------
<<<<<<< HEAD
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/alumni_network_db?useSSL=false&serverTimezone=UTC}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:admin1234}
=======
spring.datasource.url=${DATABASE_URL:jdbc:mysql://localhost:3306/alumni_network_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}
spring.datasource.username=${DATABASE_USER:root}
spring.datasource.password=${DATABASE_PASSWORD:admin1234}
>>>>>>> ef58064 (fix(deployment): Improve error handling and database configuration)

# Database connection pool settings for production
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=1200000

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# -----------------------------
# Redis Configuration
# -----------------------------
spring.data.redis.host=${REDIS_HOST
spring.data.redis.port=${REDIS_PORT}
spring.cache.type=simple
app.cache.redis-enabled=false

# -----------------------------
# JWT Configuration
# -----------------------------
jwt.secret=${JWT_SECRET:Aqwertyuioplkjhgfdsazxcvbnmkloiuytrewsasdfghjkloiuytfrdsdfghjjhgfdsghjk}
jwt.expiration=86400000

# -----------------------------
# Logging
# -----------------------------
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.web=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
logging.level.com.example.AlumniPortal=DEBUG

# -----------------------------
# Frontend Redirects
# -----------------------------
app.frontend.url=${FRONTEND_URL:http://localhost:5173}

# # -----------------------------
# # Google OAuth2
# # # IMPORTANT: Set these environment variables in production:
# # GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET, GOOGLE_REDIRECT_URI
# # ----
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET}
spring.security.oauth2.client.registration.google.scope=openid,profile,email
spring.security.oauth2.client.registration.google.redirect-uri=${GOOGLE_REDIRECT_URI}
